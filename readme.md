`
$ npx react-native start
`

`
$ npx react-native run-ios
`

`
$ npx react-devtools
`

`
$ watchman watch-del-all
`

`
$ rm -rf node_modules
`

`
$ yarn install
`

`
$ yarn start --reset-cache
`

`
$ rm -rf /tmp/metro-*
`
